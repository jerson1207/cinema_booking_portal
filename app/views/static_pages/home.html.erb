<div class="home">
  <div class="all_movies container-80">
    <% @cinema.each do |c| %>
      <% c.movies.each do |movie| %>
        <div class="card">
          <%= image_tag "movie_temp_bg.jpg"%>
          <h1><%= movie.title %> </h1>
          <p class="price"><%= c.name %></p>
          
          <%= link_to cinema_movie_path(c, movie) do %>
            <p><button>Book Now!</button></p>
          <% end %>
        </div>
        <% end %>
    <% end %>
  </div>
</div>